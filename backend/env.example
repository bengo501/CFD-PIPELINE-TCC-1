# padrao local: sqlite em local_data (ver .gitignore na raiz)
# copie para .env na raiz do repo ou ao lado do backend conforme o seu fluxo
# DATABASE_URL=sqlite:///C:/caminho/absoluto/para/repo/local_data/cfd_pipeline.db

# exemplo postgres (opcional)
# DATABASE_URL=postgresql://cfd_user:cfd_password@localhost:5432/cfd_pipeline

# seed de dados demo (1/0); por omissao ativo so para urls sqlite
# SEED_DEMO_DATA=1
