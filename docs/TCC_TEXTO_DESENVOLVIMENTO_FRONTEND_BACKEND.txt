================================================================================
TEXTO PARA TCC2 — PASSO A PASSO METODOLÓGICO DO BACKEND E DO FRONTEND
================================================================================


--------------------------------------------------------------------------------
1. PRINCÍPIOS QUE ORIENTARAM O DESENVOLVIMENTO EM DUAS CAMADAS
--------------------------------------------------------------------------------

A separação entre backend e frontend neste projeto segue o padrão arquitectural cliente–servidor habitual em aplicações web científicas: o servidor concentra regras de negócio, invocação de processos externos (compilador, Blender ou script Python, OpenFOAM), persistência e exposição de uma API estável; o cliente oferece navegação, formulários, visualização e acompanhamento de tarefas longas sem bloquear o utilizador. Metodologicamente, o passo a passo não foi linear num único sprint: alternou-se entre «fazer funcionar um fluxo mínimo» (compilar, gerar geometria, consultar estado) e «expandir superfícies» (painel, histórico, comparações, visual 3D), mantendo o contrato HTTP como fronteira explícita entre equipas mentais de trabalho.


--------------------------------------------------------------------------------
2. BACKEND — FASES DE CONSTRUÇÃO
--------------------------------------------------------------------------------

2.1. Escolha do stack e arranque do servidor

Optou-se pelo FastAPI em Python pela geração automática de documentação OpenAPI (Swagger em `/docs` e ReDoc em `/redoc`), pela validação de dados com Pydantic e pela facilidade de expor endpoints assíncronos compatíveis com tarefas de I/O e subprocessos. O ponto de entrada da aplicação é o módulo `backend.app.main`, que instancia a aplicação FastAPI, configura CORS para origens de desenvolvimento do Vite (`localhost:5173`) e monta ficheiros estáticos da pasta `generated` em `/files`, permitindo servir artefactos sem duplicar lógica de caminhos fora da API.

2.2. Modularização por routers

Em vez de um único ficheiro monolítico, as rotas organizaram-se em routers incluídos com prefixo `/api`: rotas gerais (`routes.py`: compilação, modelo, simulação, listagem de ficheiros e jobs em memória); rotas de base de dados (`routes_database.py`: leitos, simulações, resultados, ingestão de `results.json`); rotas de pipeline integrado (`routes_integrated.py`: fluxos que encadeiam compilação, geração 3D e OpenFOAM); e routers adicionais para wizard web, casos CFD, templates e rotas CFD específicas. Esta divisão metodológica espelha casos de uso distintos e reduz acoplamento: alterações no pipeline integrado não forçam reescrita dos endpoints CRUD do ORM.

2.3. Camada de serviços e integração com ferramentas externas

Por baixo dos routers, serviços encapsulam subprocessos e regras (por exemplo `BedService`, `BlenderService`, `OpenFOAMService`, `ResultsService`). O compilador `.bed` invoca o script Antlr; a geração 3D escolhe entre Blender e o script `packed_bed_stl.py` conforme perfil; a criação de caso OpenFOAM delega em `setup_openfoam_case.py`. Assim, a API permanece fina: valida entrada, dispara trabalho, regista estado em memória ou na base de dados, e devolve identificadores para consulta posterior — padrão adequado a jobs longos.

2.4. Persistência e modelos

A camada `backend/app/database` concentra conexão SQLAlchemy, modelos ORM e operações CRUD. No arranque, `create_tables()` garante esquema mínimo em desenvolvimento (com ressalva documental de que migrações formais poderiam usar Alembic em produção). Os endpoints de ingestão ligam ficheiros no disco (`results.json`) ao modelo relacional, fechando o circuito entre simulação no sistema de ficheiros e dados agregados para o dashboard.

2.5. Observabilidade e saúde do sistema

Foram expostos endpoint raiz e `/health` com verificação de conectividade à base, permitindo ao frontend mostrar estado do sistema e facilitando orquestração em Docker. A documentação interactiva do FastAPI apoia o ensaio manual e a escrita do TCC (lista de endpoints reprodutível).


--------------------------------------------------------------------------------
3. FRONTEND — FASES DE CONSTRUÇÃO
--------------------------------------------------------------------------------

3.1. Escolha do stack e arranque do cliente

O frontend adoptou React 18 com Vite como empacotador de desenvolvimento, pela rapidez de recarga e configuração mínima. O módulo `package.json` fixa dependências: `axios` para HTTP, `@react-three/fiber` e `three` para visualização 3D no browser, `recharts` para gráficos onde aplicável. O comando típico de desenvolvimento é `npm run dev`, servindo a SPA na porta padrão do Vite.

3.2. Camada de acesso à API

Centralizou-se o cliente HTTP em `frontend/src/services/api.js`, com instância Axios configurada com `baseURL` derivada de `VITE_API_URL` ou fallback para `http://localhost:8000`. Funções exportadas espelham os recursos do backend (`compileBed`, `generateModel`, `createSimulation`, `getJobStatus`, `getSimulationsSummary`, etc.), evitando URLs espalhadas pelos componentes. Metodologicamente, qualquer mudança de prefixo ou cabeçalho trata-se num único sítio — requisito importante para equipas e para o template.

3.3. Composição da interface por funcionalidade

O ficheiro `App.jsx` estrutura a navegação por separadores ou secções e importa componentes especializados: `Dashboard`, `BedForm`, `BedWizard`, `PipelineCompleto`, `CFDSimulation`, `JobStatus`, `ResultsList`, `ModelViewer`, `CasosCFD`, `ComparisonPage`, `SimulationHistory`, `TemplateEditor`, entre outros. Cada componente corresponde a um conjunto de acções do utilizador (criar leito, seguir wizard, lançar pipeline, acompanhar jobs, listar resultados). Esta fragmentação facilita manutenção e permite que utilizadores do template desactivem ou substituam telas sem reescrever toda a aplicação.

3.4. Contexto global (idioma e tema)

Contextos React (`LanguageContext`, `ThemeContext`) concentram preferências de interface (por exemplo português/inglês, tema claro/escuro), separando apresentação de chamadas à API. Do ponto de vista metodológico, isso antecipa uso por públicos mistos (laboratórios internacionais) sem alterar o contrato do backend.

3.5. Integração visual com o domínio CFD

A presença de React Three Fiber liga o frontend à necessidade de inspeccionar geometria no browser; o `ModelViewer` e componentes afins consomem ficheiros ou metadados expostos pela API ou por URLs estáticas, completando o ciclo «parâmetros → geometria → visualização» para o utilizador não especialista em linha de comandos.


--------------------------------------------------------------------------------
4. INTEGRAÇÃO BACKEND–FRONTEND E ITERAÇÃO
--------------------------------------------------------------------------------

O passo a passo de integração repetiu-se de forma incremental: (1) garantir CORS e URL base correctos; (2) implementar um fluxo ponta a ponta mínimo (compilar ou criar leito e receber `job_id`); (3) adicionar polling ou navegação para ecrã de estado de jobs; (4) ligar endpoints de resumo da base de dados ao dashboard; (5) tratar erros de rede e timeouts na camada Axios. A documentação Swagger do backend serviu de contrato vivo; o frontend ajustou corpos JSON aos modelos Pydantic. Em Docker Compose, as variáveis de ambiente alinham portas e `VITE_API_URL` ao endereço acessível pelo browser do utilizador (não pelo interior do contentor), lição metodológica recorrente em aplicações web contentorizadas.


--------------------------------------------------------------------------------
5. SÍNTESE E ENTREGÁVEL
--------------------------------------------------------------------------------

Em síntese, o backend foi construído como API modular FastAPI com serviços que fazem ponte para scripts e ferramentas do pipeline CFD, e com persistência opcional mas integrada para métricas agregadas. O frontend foi construído como SPA React modular, com camada HTTP única e componentes alinhados aos casos de uso do projecto. O resultado é um par cliente–servidor documentável, extensível e adequado a ser descrito no capítulo de «materiais e métodos» ou «implementação» do TCC2, com remissão aos ficheiros `backend/app/main.py`, `frontend/src/App.jsx` e `frontend/src/services/api.js`.


--------------------------------------------------------------------------------
6. REFERÊNCIAS TÉCNICAS (DOCUMENTAÇÃO, PARA COMPLETAR NA NORMA DA INSTITUIÇÃO)
--------------------------------------------------------------------------------

FastAPI. Documentação oficial. https://fastapi.tiangolo.com/

React. Documentação. https://react.dev/

Vite. Guia. https://vitejs.dev/

SQLAlchemy. Documentação. https://docs.sqlalchemy.org/


--------------------------------------------------------------------------------
FIM DO DOCUMENTO
--------------------------------------------------------------------------------
